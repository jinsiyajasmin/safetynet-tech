# Prisma CLI loads this file when you run commands from backend/.
# Prefer the repo root .env (see ../.env.docker.example) for Docker Compose + local dev.

DATABASE_URL=postgresql://safetyuser:safetypassword@localhost:5435/safetyapp_db?schema=public
DIRECT_URL=postgresql://safetyuser:safetypassword@localhost:5435/safetyapp_db?schema=public

# Optional — local Mailpit (docker compose) or your SMTP provider
# SMTP_HOST=localhost
# SMTP_PORT=1025
# SMTP_FROM="Sitemate" <no-reply@localhost>
